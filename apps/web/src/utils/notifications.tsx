import { errorMessage as errorMessageRoot, successMessage as successMessageRoot } from '@teleflow/design-system';

export const errorMessage = errorMessageRoot;
export const successMessage = successMessageRoot;
